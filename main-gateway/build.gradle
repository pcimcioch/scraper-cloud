apply plugin: 'spring-boot'

jar {
    baseName = 'main-gateway'
    version = '0.1.0'
}

dependencies {
    compile clientCommons

    compile "org.springframework.cloud:spring-cloud-starter-zuul:1.2.0.RELEASE"

    compile project(':common-service')
}